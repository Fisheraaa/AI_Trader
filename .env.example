# 基础配置
#ONE_API_URL=http://127.0.0.1:3000/v1/chat/completions
ONE_API_URL=http://one-api:3000/v1/chat/completions
ONE_API_TOKEN=REPLACE_WITH_YOUR_TOKEN
FEISHU_WEBHOOK=REPLACE_WITH_YOUR_WEBHOOK

# 路径配置（如果是Docker运行，建议保持默认或按需修改）
DB_PATH=data/db/trading.db
POS_FILE=my_positions.txt
LIST_FILE=etf_list.txt
KNOWLEDGE_FILE=knowledge_base.md

# 调度参数（新增）
DATA_SCRIPT=DataManager2.py
AT_SCRIPT=AT2.py
DATA_INTERVAL_SEC=3600
AT_INTERVAL_SEC=1800
SCHEDULER_HEARTBEAT_SEC=60

# 策略参数
RR_NEW_THRESHOLD=2.0
CRASH_THRESHOLD=-3.0

# LLM参数
LLM_REQUEST_TIMEOUT_S=300
LLM_TIER_TIMEOUT_S=360
LLM_PRIMARY_ATTEMPTS=3
LLM_PRIMARY_BACKOFF_S=8

LLM_TEMPERATURE=0.0
QUANT_WEIGHT=0.65
AI_WEIGHT=0.35

# 数据管理参数
DM_BATCH_SIZE=20
DM_BATCH_SLEEP_SEC=25
DM_MAX_ROUNDS=8
DM_HTTP_TIMEOUT=15

# 其他参数
DAILY_FULL_HOUR=16
DAILY_FULL_MIN=10

DM_HOURLY_TIMEOUT_SEC=5400
DM_FULL_TIMEOUT_SEC=7200
AT_TIMEOUT_SEC=1200